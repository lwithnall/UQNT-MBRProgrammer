import { useEffect } from "react";
import { BlockMirror } from "../lib/src/block_mirror";

export function BlockEditor() {
  useEffect(() => {
  // const blockMirror = new BlockMirror();
  }, [])

  return <div className="bg-red-500">Hello</div>;
}